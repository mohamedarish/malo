import { vowels, consonants, compounds, modifiers, chillaksharam } from "./data";

const en2ml = (word: string): string[] => {
	const res: string[] = [];

	for (let i = 0; i < word.length; i += 1) {
		
	}

	return res;
};

// console.log(en2ml("omana"));

export default en2ml;
